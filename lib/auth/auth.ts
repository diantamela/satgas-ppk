// Custom authentication system - Better Auth disabled
// We're using custom signin/signup routes instead

export const auth = {
    // Placeholder for compatibility - we're not using Better Auth
    api: {
        getSession: async () => null,
    }
};